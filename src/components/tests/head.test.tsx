import React from "react";
import renderer from "react-test-renderer";
import Head from "../Head";

test.todo("Need to make test for Head component");
// test("Language set to English", () => {
//   const head = renderer.create(
//     <Head title="Jack Moody" description="This is a short description."></Head>
//   );
//   console.log(head.toJSON());
// });
